{
  "//": "Default Cynara API configuration. Connection strings and other secrets MUST come from environment variables (ConnectionStrings__Default, OPENAI_API_KEY, etc.) — never from this file. For local development the defaults below point at the docker-compose Postgres stack; in any deployed environment the corresponding env var on the host overrides these values.",

  "AllowedHosts": "*",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "Cors": {
    "//": "Origins allowed to call the API via browser fetch / XHR. Replace with the real cynara-web origin in the environment-specific override.",
    "AllowedOrigins": [
      "http://localhost:3000",
      "http://localhost:5173"
    ]
  },

  "ConnectionStrings": {
    "//": "Npgsql connection string. The local docker-compose stack publishes 5432 on host. In non-development environments set the ConnectionStrings__Default environment variable instead — it overrides this value and never needs to be checked in.",
    "Default": "Host=localhost;Port=5432;Database=postgres;Username=postgres;Password=postgres"
  },

  "Hospitals": {
    "//": "Bootstrap workspace created on first boot when AllowAutoBootstrap is true. HeaderName is the request header used to resolve the hospital tenant on every call.",
    "BootstrapCode": "default",
    "BootstrapName": "Default workspace",
    "HeaderName": "X-Hospital-Code",
    "AllowAutoBootstrap": true
  }
}
