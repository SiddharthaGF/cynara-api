name: Deploy preview

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: hello
        run: echo "hello from $(uname -a)"
